<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management System</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        /* Simple CSS with .NET compatibility */
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
            --success: #28a745;
            --warning: #ffc107;
            --danger: #dc3545;
            --light: #f8f9fa;
            --dark: #343a40;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f5f5f5;
        }

        /* Header */
        .navbar-brand {
            font-weight: 600;
            font-size: 1.5rem;
        }

        .navbar {
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }

        /* Sidebar */
        .sidebar {
            background: white;
            height: calc(100vh - 56px);
            position: fixed;
            left: 0;
            top: 56px;
            width: 250px;
            box-shadow: 2px 0 5px rgba(0,0,0,.1);
            overflow-y: auto;
            z-index: 100;
        }

        .sidebar-menu {
            list-style: none;
            padding: 0;
            margin: 0;
        }

            .sidebar-menu li {
                margin: 5px 0;
            }

            .sidebar-menu a {
                display: block;
                padding: 12px 20px;
                color: var(--dark);
                text-decoration: none;
                border-left: 3px solid transparent;
                transition: all 0.3s;
            }

                .sidebar-menu a:hover,
                .sidebar-menu a.active {
                    background: #e9ecef;
                    border-left-color: var(--secondary);
                    color: var(--secondary);
                }

            .sidebar-menu i {
                width: 24px;
                text-align: center;
                margin-right: 10px;
            }

        /* Main Content */
        .main-content {
            margin-left: 20px;
            padding: 20px;
            margin-top: 56px;
            transition: margin-left 0.3s;
        }

        /* Cards */
        .dashboard-card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,.1);
            transition: transform 0.3s;
            height: 100%;
        }

            .dashboard-card:hover {
                transform: translateY(-5px);
            }

        .card-icon {
            width: 60px;
            height: 60px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: white;
            margin-bottom: 15px;
        }

        /* Tables */
        .data-table {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,.1);
            overflow: hidden;
        }

        .table thead th {
            border-bottom: 2px solid #dee2e6;
            background: #f8f9fa;
            font-weight: 600;
        }

        /* Badges */
        .status-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        /* Search Box */
        .search-container {
            position: relative;
        }

            .search-container i {
                position: absolute;
                left: 15px;
                top: 50%;
                transform: translateY(-50%);
                color: #6c757d;
            }

        .search-input {
            padding-left: 40px;
            border-radius: 20px;
            border: 1px solid #ced4da;
        }

        /* Mobile Menu Toggle */
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 24px;
            color: var(--primary);
        }

        /* Media Queries for Responsiveness */
        @@media (max-width: 992px) {
            .sidebar

        {
            left: -250px;
            transition: left 0.3s;
        }

        .sidebar.active {
            left: 0;
        }

        .main-content {
            margin-left: 0;
        }

        .mobile-menu-btn {
            display: block;
        }

        }

        @@media (max-width: 768px) {
            .main-content

        {
            padding: 15px;
        }

        .card {
            margin-bottom: 15px;
        }

        }

        @@media (max-width: 576px) {
            .navbar-brand span

        {
            display: none;
        }

        .search-container {
            width: 100%;
            margin-top: 10px;
        }

        }

        /* Print Styles */
        @@media print {
            .sidebar, .navbar, .btn, .mobile-menu-btn

        {
            display: none !important;
        }

        .main-content {
            margin: 0;
            padding: 0;
        }

        .card {
            box-shadow: none !important;
            border: 1px solid #dee2e6 !important;
        }

        }

        /* Focus Styles for Accessibility */
        a:focus,
        button:focus,
        input:focus,
        select:focus {
            outline: 2px solid var(--secondary);
            outline-offset: 2px;
        }

        /* Loading Spinner */
        .spinner-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100px;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid var(--secondary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @@keyframes spin {
            0%

        {
            transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }

        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        ::-webkit-scrollbar-thumb {
            background: #c1c1c1;
            border-radius: 4px;
        }

            ::-webkit-scrollbar-thumb:hover {
                background: #a8a8a8;
            }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
        <div class="container-fluid">
            <button class="mobile-menu-btn" id="mobileMenuBtn">
                <i class="bi bi-list"></i>
            </button>

            <a class="navbar-brand ms-2" href="#">
                <i class="bi bi-book-half me-2"></i>
                <span>Library Management System</span>
            </a>

            <div class="d-flex align-items-center">
                <div class="search-container me-3">
                    <i class="bi bi-search"></i>
                    <input type="text" class="form-control search-input" placeholder="Search..."
                           id="searchInput" style="width: 250px;">
                </div>

                <div class="dropdown">
                    <button class="btn btn-light dropdown-toggle" type="button" id="userDropdown"
                            data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-2"></i>
                        Admin
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i>Profile</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-gear me-2"></i>Settings</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="p-3">
            <ul class="sidebar-menu">
                <li><a href="#" class="active"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                <li><a href="#"><i class="bi bi-book"></i> Books Management</a></li>
                <li><a href="#"><i class="bi bi-people"></i> Members</a></li>
                <li><a href="#"><i class="bi bi-arrow-left-right"></i> Transactions</a></li>
                <li><a href="#"><i class="bi bi-calendar-check"></i> Reservations</a></li>
                <li><a href="#"><i class="bi bi-clock-history"></i> Overdue</a></li>
                <li><a href="#"><i class="bi bi-bar-chart"></i> Reports</a></li>
                <li><a href="#"><i class="bi bi-gear"></i> Settings</a></li>
            </ul>

            <div class="mt-4 p-3 bg-light rounded">
                <small class="text-muted">Quick Stats</small>
                <div class="d-flex justify-content-between mt-2">
                    <span>Available Books</span>
                    <strong>8,245</strong>
                </div>
                <div class="d-flex justify-content-between">
                    <span>Active Members</span>
                    <strong>1,024</strong>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-content" id="mainContent">
        <!-- Page Title -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="mb-0">Dashboard Overview</h3>
            <div>
                <button class="btn btn-primary me-2"><i class="bi bi-plus-circle me-1"></i> Add New</button>
                <button class="btn btn-outline-secondary"><i class="bi bi-printer me-1"></i> Print</button>
            </div>
        </div>

        <!-- Statistics Cards -->
        <div class="row g-4 mb-4">
            <div class="col-md-3 col-sm-6">
                <div class="card dashboard-card">
                    <div class="card-body">
                        <div class="card-icon bg-primary">
                            <i class="bi bi-book"></i>
                        </div>
                        <h5 class="card-title">Total Books</h5>
                        <h2 class="mb-0">12,458</h2>
                        <p class="text-success mb-0"><small><i class="bi bi-arrow-up"></i> 5.2% increase</small></p>
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="card dashboard-card">
                    <div class="card-body">
                        <div class="card-icon bg-success">
                            <i class="bi bi-people"></i>
                        </div>
                        <h5 class="card-title">Active Members</h5>
                        <h2 class="mb-0">1,248</h2>
                        <p class="text-success mb-0"><small><i class="bi bi-arrow-up"></i> 8.7% increase</small></p>
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="card dashboard-card">
                    <div class="card-body">
                        <div class="card-icon bg-warning">
                            <i class="bi bi-arrow-left-right"></i>
                        </div>
                        <h5 class="card-title">Books on Loan</h5>
                        <h2 class="mb-0">324</h2>
                        <p class="text-danger mb-0"><small><i class="bi bi-arrow-down"></i> 2.3% decrease</small></p>
                    </div>
                </div>
            </div>

            <div class="col-md-3 col-sm-6">
                <div class="card dashboard-card">
                    <div class="card-body">
                        <div class="card-icon bg-danger">
                            <i class="bi bi-exclamation-triangle"></i>
                        </div>
                        <h5 class="card-title">Overdue Books</h5>
                        <h2 class="mb-0">18</h2>
                        <p class="text-danger mb-0"><small>Needs attention</small></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Recent Activity Tables -->
        <div class="row g-4">
            <div class="col-lg-6">
                <div class="data-table">
                    <div class="p-3 border-bottom">
                        <h5 class="mb-0">Recent Book Loans</h5>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th>Book Title</th>
                                    <th>Member</th>
                                    <th>Due Date</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>The Great Gatsby</td>
                                    <td>John Smith</td>
                                    <td>15-10-2023</td>
                                    <td><span class="status-badge bg-success text-white">On Time</span></td>
                                </tr>
                                <tr>
                                    <td>To Kill a Mockingbird</td>
                                    <td>Sarah Johnson</td>
                                    <td>10-10-2023</td>
                                    <td><span class="status-badge bg-danger text-white">Overdue</span></td>
                                </tr>
                                <tr>
                                    <td>1984</td>
                                    <td>Michael Brown</td>
                                    <td>20-10-2023</td>
                                    <td><span class="status-badge bg-success text-white">On Time</span></td>
                                </tr>
                                <tr>
                                    <td>Pride and Prejudice</td>
                                    <td>Emily Davis</td>
                                    <td>12-10-2023</td>
                                    <td><span class="status-badge bg-warning text-dark">Due Soon</span></td>
                                </tr>
                                <tr>
                                    <td>The Catcher in the Rye</td>
                                    <td>Robert Wilson</td>
                                    <td>25-10-2023</td>
                                    <td><span class="status-badge bg-success text-white">On Time</span></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <div class="col-lg-6">
                <div class="data-table">
                    <div class="p-3 border-bottom">
                        <h5 class="mb-0">Recently Added Books</h5>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th>Title</th>
                                    <th>Author</th>
                                    <th>Category</th>
                                    <th>Added Date</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>The Midnight Library</td>
                                    <td>Matt Haig</td>
                                    <td>Fiction</td>
                                    <td>05-10-2023</td>
                                </tr>
                                <tr>
                                    <td>Atomic Habits</td>
                                    <td>James Clear</td>
                                    <td>Self-Help</td>
                                    <td>03-10-2023</td>
                                </tr>
                                <tr>
                                    <td>Klara and the Sun</td>
                                    <td>Kazuo Ishiguro</td>
                                    <td>Science Fiction</td>
                                    <td>01-10-2023</td>
                                </tr>
                                <tr>
                                    <td>Project Hail Mary</td>
                                    <td>Andy Weir</td>
                                    <td>Science Fiction</td>
                                    <td>28-09-2023</td>
                                </tr>
                                <tr>
                                    <td>The Code Breaker</td>
                                    <td>Walter Isaacson</td>
                                    <td>Biography</td>
                                    <td>25-09-2023</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="row g-4 mt-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title mb-4">Quick Actions</h5>
                        <div class="row g-3">
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-primary w-100 py-3">
                                    <i class="bi bi-plus-circle d-block mb-2" style="font-size: 24px;"></i>
                                    Add Book
                                </button>
                            </div>
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-success w-100 py-3">
                                    <i class="bi bi-person-plus d-block mb-2" style="font-size: 24px;"></i>
                                    New Member
                                </button>
                            </div>
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-warning w-100 py-3">
                                    <i class="bi bi-arrow-left-right d-block mb-2" style="font-size: 24px;"></i>
                                    Issue Book
                                </button>
                            </div>
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-info w-100 py-3">
                                    <i class="bi bi-clock-history d-block mb-2" style="font-size: 24px;"></i>
                                    Check Overdue
                                </button>
                            </div>
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-secondary w-100 py-3">
                                    <i class="bi bi-printer d-block mb-2" style="font-size: 24px;"></i>
                                    Print Reports
                                </button>
                            </div>
                            <div class="col-md-2 col-sm-4 col-6">
                                <button class="btn btn-outline-dark w-100 py-3">
                                    <i class="bi bi-gear d-block mb-2" style="font-size: 24px;"></i>
                                    Settings
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer class="mt-5 pt-4 border-top">
            <div class="row">
                <div class="col-md-6">
                    <p class="text-muted mb-0">
                        <i class="bi bi-c-circle me-1"></i> 2023 Library Management System
                    </p>
                </div>
                <div class="col-md-6 text-md-end">
                    <p class="text-muted mb-0">
                        <i class="bi bi-clock me-1"></i> Last updated: 10-10-2023 |
                        <i class="bi bi-shield-check ms-2 me-1"></i> Version: 2.1.0
                    </p>
                </div>
            </div>
        </footer>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Simple JavaScript for .NET compatibility

        // Mobile Menu Toggle
        document.getElementById('mobileMenuBtn').addEventListener('click', function() {
            document.getElementById('sidebar').classList.toggle('active');
        });

        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const menuBtn = document.getElementById('mobileMenuBtn');
            const isMobile = window.innerWidth <= 992;

            if (isMobile && sidebar.classList.contains('active') &&
                !sidebar.contains(event.target) &&
                !menuBtn.contains(event.target)) {
                sidebar.classList.remove('active');
            }
        });

        // Search functionality with Enter key support
        document.getElementById('searchInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                const searchTerm = this.value.trim();
                if (searchTerm) {
                    alert('Searching for: ' + searchTerm);
                    // In .NET, you would submit form or make AJAX call here
                    // window.location.href = '/Search?term=' + encodeURIComponent(searchTerm);
                }
            }
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            // Ctrl + / for search focus
            if (e.ctrlKey && e.key === '/') {
                e.preventDefault();
                document.getElementById('searchInput').focus();
            }

            // Escape to clear search
            if (e.key === 'Escape') {
                document.getElementById('searchInput').value = '';
                document.getElementById('searchInput').blur();
            }

            // Ctrl + M for mobile menu toggle (on mobile/tablet)
            if (e.ctrlKey && e.key === 'm' && window.innerWidth <= 992) {
                e.preventDefault();
                document.getElementById('sidebar').classList.toggle('active');
            }
        });

        // Window resize handler
        window.addEventListener('resize', function() {
            const sidebar = document.getElementById('sidebar');
            if (window.innerWidth > 992) {
                sidebar.classList.remove('active');
            }
        });

        // Simple loading simulation
        function showLoading() {
            const mainContent = document.getElementById('mainContent');
            mainContent.innerHTML = `
                <div class="spinner-container">
                    <div class="spinner"></div>
                </div>
            `;

            // Simulate data loading
            setTimeout(() => {
                location.reload(); // In .NET, this would be partial page update
            }, 1000);
        }

        // Add click handlers to action buttons
        document.querySelectorAll('.btn-outline-primary, .btn-outline-success, .btn-outline-warning').forEach(btn => {
            btn.addEventListener('click', function() {
                const action = this.textContent.trim();
                alert('Action: ' + action + '\n\nIn .NET, this would open a modal or redirect to a form page.');
            });
        });

        // Table row click handlers
        document.querySelectorAll('tbody tr').forEach(row => {
            row.addEventListener('click', function() {
                // Remove previous selection
                document.querySelectorAll('tbody tr').forEach(r => {
                    r.classList.remove('table-active');
                });

                // Add selection to clicked row
                this.classList.add('table-active');

                // In .NET, you would get the data-id and load details
                const cells = this.cells;
                if (cells.length > 0) {
                    console.log('Selected:', cells[0].textContent);
                }
            });
        });

        // Print button functionality
        document.querySelector('.btn-outline-secondary').addEventListener('click', function() {
            window.print();
        });

        // Initialize tooltips (Bootstrap feature)
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl);
        });

        // Media query detection for debugging
        function checkMediaQueries() {
            console.log('Window width:', window.innerWidth);
            console.log('Is mobile:', window.innerWidth <= 992);
        }

        // Check on load
        window.addEventListener('load', checkMediaQueries);

        // Check on resize
        window.addEventListener('resize', checkMediaQueries);
    </script>
</body>
</html>